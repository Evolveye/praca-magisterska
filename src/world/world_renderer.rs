use crate::rendering::{
  model_n_buffers::Model, renderer::Renderer
};

pub struct WorldRenderer {
  // renderer:Renderer
}

impl WorldRenderer {
  // pub fn new(renderer:&mut Renderer) -> Self {
  //   let world_renderer = Self {};

  //   let _ = renderer.register_model( &world_renderer.update_command_buffer );

  //   world_renderer
  // }


  pub fn render( &mut self, renderer:&mut Renderer ) {
    // renderer.;
  }

  // pub fn render( &mut self ) {
  //   // self.renderer.
  // }

  fn update_command_buffer( &mut self ) {

  }
}
